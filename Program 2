2. Write a C program to calculate the electricity bill for a consumer based on the following 
tariff rules:
#include <stdio.h>
int main() 
{
 char name[50];
 int units, 
float charges = 0, total;
printf (“\n Electricity Bill Application \n”);
printf (“-------------------------------------------------------------------------”);
 printf ("Enter customer name: ");
 scanf ("%s", name);
 printf ("Enter number of units consumed: ");
 scanf ("%d", &units);
 // Calculate charges based on units
 if (units <= 200)
{
 charges = units * 0.80;
}
else if (units <= 300) 
 {
 charges = (200 * 0.80) + (units - 200) * 0.90;
 } 
else 
{
 charges = (200 * 0.80) + (100 * 0.90) + (units - 300) * 1.00;
 }
 total = charges + 100; // Add minimum meter charge
 if (total > 400) // Additional surcharge if total > 400
{
 total += total * 0.15; // 15% surcharge
 }
// Print output
 printf ("\n Electricity Bill\n");
 printf ("---------------------\n");
 printf ("Customer Name: %s\n", name);
 printf ("Units Consumed: %.d\n", units);
 printf ("Total Charges: Rs %.2f\n", total);
}
