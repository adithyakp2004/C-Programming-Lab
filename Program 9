Write a C-program to copy a text file to another, read both the input filename and 
target filename.

#include <stdio.h>
#include <stdlib.h>
int main() 
{
 FILE *sourceFile, *targetFile;
 char ch;
 // Open the source file in read mode
 sourceFile = fopen ( “D:\Demo\sourceSample.txt”, "r");
 if (sourceFile == NULL) 
{
 printf ("Error: Cannot open source file!\n");
 exit (1);
 }
 // Open (or create) the target file in write mode
 targetFile = fopen (“d:\Demo\destSample.txt”, "w");
 if (targetFile == NULL) 
{
 printf ("Error: Cannot open target file!\n");
 fclose (sourceFile); 
// Close source if target fails
 exit (1);
 }
 // Copy contents character by character
 while ((ch = fgetc (sourceFile)) != EOF) 
{
 fputc (ch, targetFile);
 }
 printf ("File copied successfully.\n");
 fclose (sourceFile);
 fclose (targetFile);
}
